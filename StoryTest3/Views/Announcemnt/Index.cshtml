@model IEnumerable<StoryTest3.Models.Announcement>

@{
    ViewBag.Title = "公告首頁";
}

    <h2>公告首頁</h2>

<p>
    @Html.ActionLink("新增公告", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PostName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PostTime)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.PostCont)
        </th>*@
        @*<th>
            @Html.DisplayNameFor(model => model.Administrator.AdminAccount)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PostName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PostTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PostCont)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Administrator.AdminAccount)
        </td>
        <td>
            @Html.ActionLink("修改", "Edit", new { id=item.PostID }) |
            @Html.ActionLink("公告內文", "Details", new { id=item.PostID }) |
            @Html.ActionLink("刪除", "Delete", new { id=item.PostID })
        </td>
    </tr>
}

</table>
